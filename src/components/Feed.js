import { makeStyles } from '@mui/material';
import { theme } from './theme';

const useStyles = makeStyles({

});

function Feed() {
  return (
    <div className="App">
      
    </div>
  );
}

export default Feed;